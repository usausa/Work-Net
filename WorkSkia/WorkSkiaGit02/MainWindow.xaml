<Window x:Class="WorkSkiaGit02.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkSkiaGit02"
        xmlns:controls="clr-namespace:WorkSkiaGit02.Controls"
        xmlns:conv="clr-namespace:WorkSkiaGit02.Converters"
        mc:Ignorable="d"
        Title="Git Graph (per-row ListBox)" Height="800" Width="1280"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Window.Resources>
        <conv:RefKindBrushConverter x:Key="RefBackgroundConverter" Foreground="False" />
        <conv:RefKindBrushConverter x:Key="RefForegroundConverter" Foreground="True" />

        <Style x:Key="MetaTextStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="#FF666666" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <Style x:Key="SummaryTextStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="ShaTextStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="#FF888888" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="12" />
        </Style>

        <DataTemplate x:Key="RefBadgeTemplate">
            <Border Margin="0,0,4,0"
                    Padding="6,1"
                    CornerRadius="4"
                    BorderThickness="1"
                    BorderBrush="#FF333333"
                    Background="{Binding Kind, Converter={StaticResource RefBackgroundConverter}}">
                <TextBlock Text="{Binding Name}"
                           FontFamily="Segoe UI"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="{Binding Kind, Converter={StaticResource RefForegroundConverter}}" />
            </Border>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#FF2D2D30" Padding="10,6">
            <TextBlock x:Name="HeaderText"
                       Foreground="White"
                       FontFamily="Segoe UI"
                       FontSize="13"
                       Text="Loading..." />
        </Border>

        <ListBox x:Name="RowList"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 BorderThickness="0"
                 Padding="0"
                 HorizontalContentAlignment="Stretch"
                 FontFamily="Segoe UI"
                 FontSize="12">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="SnapsToDevicePixels" Value="True" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        Padding="0"
                                        BorderThickness="0"
                                        SnapsToDevicePixels="True">
                                    <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#FFF5F7FA" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#FFD7E6F8" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Height="26" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <controls:GraphRowSurface Grid.Column="0"
                                                  RowData="{Binding}"
                                                  VerticalAlignment="Stretch"
                                                  SnapsToDevicePixels="True" />

                        <ItemsControl Grid.Column="1"
                                      Margin="6,0,4,0"
                                      VerticalAlignment="Center"
                                      ItemsSource="{Binding Refs}"
                                      ItemTemplate="{StaticResource RefBadgeTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>

                        <TextBlock Grid.Column="2"
                                   Margin="0,0,8,0"
                                   Style="{StaticResource ShaTextStyle}"
                                   Text="{Binding ShortSha}" />

                        <TextBlock Grid.Column="3"
                                   Margin="0,0,16,0"
                                   Style="{StaticResource SummaryTextStyle}"
                                   Text="{Binding Summary}" />

                        <TextBlock Grid.Column="4"
                                   Margin="0,0,16,0"
                                   Style="{StaticResource MetaTextStyle}"
                                   Text="{Binding Author}" />

                        <TextBlock Grid.Column="5"
                                   Margin="0,0,16,0"
                                   Style="{StaticResource MetaTextStyle}"
                                   Text="{Binding When, StringFormat='yyyy-MM-dd HH:mm'}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Window>
